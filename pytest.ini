[pytest]
# Each demo-app has its own test suite that should be run independently.
# Run from project root: pytest demo-app/tests/ -v
# Or: pytest demo-app-jinja/tests/ -v
# Do NOT run both at once (module name collision by design).
